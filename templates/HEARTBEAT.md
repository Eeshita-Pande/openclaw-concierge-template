# HEARTBEAT.md

## Checks (rotate through these)

1. Review `memory/shared/diffs/` for pending Fabric diffs older than 24h → auto-approve
2. Check recent `memory/shared/daily/` logs — anything worth promoting to MEMORY.md?
3. Git auto-commit if workspace has uncommitted changes
